CREATE TABLE IF NOT EXISTS TASK (
                                    ID BIGINT PRIMARY KEY AUTO_INCREMENT,
                                    TITLE VARCHAR(255),
    DESCRIPTION VARCHAR(255),
    STATUS VARCHAR(50)
    );